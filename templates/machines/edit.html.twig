<!DOCTYPE html>

<title>Edit Machines</title>

{% block body %}
    <h1>Edit Machines</h1>

    {{ include('machines/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('machines_index') }}">back to list</a>

    {{ include('machines/_delete_form.html.twig') }}
{% endblock %}
