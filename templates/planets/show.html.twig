<!DOCTYPE html>

<title>Planets</title>

{% block body %}
    <h1>Planets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ planet.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planets_index') }}">back to list</a>

    <a href="{{ path('planets_edit', {'id': planet.id}) }}">edit</a>

    {{ include('planets/_delete_form.html.twig') }}
{% endblock %}
